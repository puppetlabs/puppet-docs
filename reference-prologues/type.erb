Type Reference (<%= version %>)
=======================

This reference covers the built-in resource types for Puppet version
<%= version %>, including details like:

* Parameters 
* Providers
* Features

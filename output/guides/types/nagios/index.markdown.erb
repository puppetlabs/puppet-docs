Nagios Types
============

* These types generate Nagios configuration statements in
Nagios-parseable configuration files.
* Autogenerated using the model developed in
[Naginator](http://projects.puppetlabs.com/projects/naginator).

* * *

Important Notes
---------------

WARNING: You can purge Nagios resources using the [`resources`](../resources.html) type, but
*only* in the default file locations. This is an architectural
limitation.

Types
-----
<%- page.parent.children.each do |e| -%>
<%- if e != page -%>
<%- if e.to_s =~ /markdown$/ -%>
* [<%= e.basename('.markdown') %>](<%= e.basename('.markdown') %>.html)
<%- elsif e.directory? -%>
* [<%= e.basename %>](<%= e.basename %>/) types
<%- end -%>
<%- end -%>
<%- end -%>
